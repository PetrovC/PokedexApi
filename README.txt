steps:
            run cmd on the root of directory

            (install node_modules and angular material)
            1.npm install
            2.ng add @angular/material
            
            (run angular app )
            3.ng s --open 